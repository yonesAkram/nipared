pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    docker version
                    docker ps
            }
        }
    }
}
}
