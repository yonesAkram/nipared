pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                        def version = "1.0.0" 
                        echo "Building version ${version}"
            }
        }
    }
}
}
