pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    sh 'docker version'
                    sh 'docker ps'
                }
            }
        }
    }
}

